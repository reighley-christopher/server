get badzork to work in /etc/init.d
consolidate reighley-core.js and crutils.js in www/static
make the node servers more compatible with npm :
  we shouldn't need two installations of express and promise, just one at srv root
  .js files need license information etc.
  package.json files to ease installation.
